# events_log.csv(약 23MB)는 GitHub 권장 파일 크기(50MB 경고 기준)에는 걸리지 않지만
# git diff/clone 성능을 위해 Git LFS로 관리합니다.
# 새 컴퓨터에서 클론 시 `git lfs install` 먼저 실행 필요.
data/generated/events_log.csv filter=lfs diff=lfs merge=lfs -text
data/generated/orders.csv filter=lfs diff=lfs merge=lfs -text
