# events_log.csv(약 23MB)·orders.csv(약 1MB)는 GitHub 하드 제한(100MB)에는 걸리지 않아
# 일반 git 파일로 관리합니다. 원래는 git diff/clone 성능을 위해 Git LFS로 관리할 계획이었으나,
# 배포 환경의 아웃바운드 정책이 GitHub LFS의 필수 확인(verify) 엔드포인트(lfs.github.com)를
# 차단하고 있어 LFS push를 완료할 수 없어 일반 파일 방식으로 전환했습니다.
# (자세한 내용은 README.md의 "Git LFS 대신 일반 파일로 관리하는 이유" 참고)
