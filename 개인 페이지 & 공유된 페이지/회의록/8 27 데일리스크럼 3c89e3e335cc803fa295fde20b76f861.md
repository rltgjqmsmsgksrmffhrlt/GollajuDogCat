# 8/27 데일리스크럼

해당 스프린트: Sprint 2
유형: 데일리 스크럼
과정: 백엔드
날짜: 2026년 8월 27일

## 1. 오늘의 목표 및 계획

---

**백엔드 오전 데일리 스크럼**

- 브런치명 변경에 따른 내용
    
    #### **The default branch has been renamed!**
    
    develop is now named dev
    
    If you have a local clone, you can update it by running the following commands.
    
    ```
    git branch -m develop dev
    git fetch origin
    git branch -u origin/dev dev
    git remote set-head origin -a
    ```
    
- ERD https://www.erdcloud.com/d/vY49e79WKRJQgX7d6
- 기능 정리 메모[기능 정리 메모](%EA%B8%B0%EB%8A%A5%20%EC%A0%95%EB%A6%AC%20%EB%A9%94%EB%AA%A8%203c89e3e335cc807981e2ed453c6e3b4d.md)

### 공통

- [ ]  멘토링 질문 있으면 작성

### 문시원

- [x]  PG사 선정
- [ ]  기능 정리 및 테이블 설계 진행

### 노여진

- [ ]  IA기반 정리
- [ ]  API 스펙 정리

## 2. 상황 공유

---

### 오전 팀장 데일리 스크럼 요약

- 전달할 사항
    - `PM` 백오피스 로직 요구
    - `인프라` 헬스체크 API의 오류 응답 관련 문제 없는지
- PG사 선정 필요 ← 토스페이먼츠로 결정

### 백엔드 공유 사항

[AI 통합 데이터 스키마 — 백엔드 전달용](https://app.notion.com/p/AI-3c39e3e335cc80bca8cbda88b1fc2fed?pvs=21) 

- 백엔드 확정 요청사항
    
    ```markup
    ## 9. 백엔드 확정 요청사항
    
    1. 기존 users·상품·카테고리·브랜드 테이블과 이 문서의 공통 필드 매핑을 확정한다.
    2. 인증·권한·연락처 등 회원 필드는 백엔드에서 자율 구성하고 AI에는 user_id만 제공한다.
    3. product_group_id 생성·병합·분리 기준과 기존 상품 마이그레이션 방식을 확정한다.
    4. pet_profile.birth_date 필수화와 추정 생일 표시 방식을 확정한다.
    5. allergy_codes, ingredients, allergen_flags, allergen_ingredient_map이 동일 코드 체계를 사용하도록 합의한다.
    6. 다견·다묘 가정에서 `behavior_events.pet_id`와 `order_items.pet_id`의 귀속 방식 및 다중 반려동물 사용 여부를 결정한다.
    7. 주문 취소·반품 처리와 상품군·카테고리·반복소비 여부·용량 스냅샷 저장 방식을 확정한다.
    8. 합성 주문·행동·리뷰 데이터는 운영 DB와 분리하고 dataset_generation_runs를 통해 생성 버전을 추적한다.
    9. 리뷰의 허용 출처·수집 범위·원문 보관·사용 범위를 확인한다.
    10. pgvector 확장 사용 여부와 임베딩 차원·인덱스 방식을 확정한다.
    11. AAFCO·NIAS 기준 시드의 공식 출처와 개정 버전 관리 방식을 확정한다.
    12. recommendation_runs·recommendation_items를 영구 저장할지 평가·로그 환경에만 보존할지 결정한다.
    13. Phase 2 피드백 테이블 구현 시점과 전환 질문 노출 기준 횟수를 확정한다.
    14. 각 테이블의 최종 PK·FK·UNIQUE·CHECK·인덱스 및 초기 데이터 적재 책임을 확정한다.
    15. ONE_TIME·SUBSCRIPTION 주문 구분과 기존 구독 엔티티의 subscription_id 연동 방식을 확정한다.
    16. 할인 전 단가·할인 금액·프로모션 식별자의 원천과 적재 단위를 확정한다.
    17. repurchase_training_samples를 DB View·Materialized View·오프라인 파일 중 어떤 형태로 제공할지 AI·백엔드가 합의한다.
    18. probability_7d·probability_14d·probability_30d와 confidence_grade의 API 타입 및 null 처리 정책을 확정한다.
    
    18-1. 재구매 목데이터 생성 시 synthetic_repurchase_profiles·synthetic_repurchase_scenarios를 개발·스테이징에 함께 적재하고 운영·모델 입력에서는 제외하는 방식을 확정한다.
    
    18-2. 초기 ORDER 생성 기준은 order_items 100,000건, 관측기간 18~24개월이며, 분포 허용범위와 테이블별 실제 행 수를 dataset_generation_runs에 저장한다.
    
    1. POST /v1/feeds/analyze 요청·응답 DTO의 enum, null 허용, 오류 응답 규격을 영양 파트와 확정한다.
    2. excluded_reasons 코드 목록과 persona_friendly.easy_terms의 관리 위치·버전 관리 방식을 확정한다.
    3. 영양 분석 결과는 현재 API 응답 계약으로만 사용하며 영구 저장 또는 평가 로그 적재가 필요하면 별도 출력 테이블을 추가한다.
    ```
    
- AI파트 전달 노션 (9. 백엔드 확정 요청사항)
    
    ### A. 백엔드 혼자 결정하면 되는 것 (AI 확인 불필요)
    
    - **2번**: 이미 답 나와있음 — user_id만 제공, 나머지 회원 필드는 백엔드 자율.
    - **3번**: product_group 생성/병합 기준 — 상품 도메인 설계 문제
    - **6번**: 다견/다묘 pet_id 처리 방식 — 지난번 `order_item_pets` 방식으로 이미 방향 잡음
    - **7번**: 주문 취소/반품 처리 방식 — 이커머스 도메인 설계
    - **14번**: PK/FK/인덱스 최종 확정 — 지금까지 논의한 Long/UUID 절충안 적용
    - **15번**: 구독 연동 방식 — 구독 담당 팀원과 협의(AI 아님)
    - **16번**: 할인/프로모션 저장 단위 — 이커머스 도메인 설계
    
    ### B. AI팀과 같이 정해야 하는 것 (진짜 협의 필요)
    
    - **1번**: 백엔드 테이블과 필드 매핑
    - **4번**: birth_date 필수화
    - **5번**: 알레르기/원료 코드 체계 통일
    - **9번**: 리뷰 출처/수집 범위
    - **10번**: pgvector 사용 여부
    - **17번**: repurchase_training_samples를 View로 줄지 파일로 줄지
    - **18번**: 확률값 API 타입/null 처리
    
    ### C.  원칙만 지키면 되는 것 (별도 결정 불필요)
    
    - **8번, 18-1**: 합성데이터는 운영DB와 분리 — 방금 정리한 "개발/스테이징 전용 스키마" 구조로 이미 해결됨
    - **18-2**: order_items 10만건 기준 — 이미 확인됨
    - **11번**: AAFCO/NIAS 기준 시드 출처
    
    ### D. 영양 파트 전용 (완전 별도 트랙, 다른 사람과 협의)
    
    - 영양파트 1, 2, 3번 — `POST /v1/feeds/analyze` API 스펙, 이건 재구매/추천이 아니라 **영양 분석 담당 AI 팀원**과 별도로 맞춰야 하는 부분
    
    ### E. 지금 당장 안 봐도 되는 것 (Phase 2, 나중 일)
    
    - **12번**: recommendation 영구저장 여부 — MVP 이후 고민해도 됨
    - **13번**: Phase 2 피드백 테이블 — 이름 그대로 나중 일

기타

- 확정 사항은 [API 명세 초안](https://app.notion.com/p/API-3bb9e3e335cc800f89a7da87c1a4cc48?pvs=21) 문서에 바로 정리하기
늦어도 28일 오전까지 작성해보기(현재 PRD, IA, 유즈케이스 기반)
- 추후 프론트엔드와 확정 필요 (API 문서 정리되면 전달드리기로 함)
    - 중요도 등도 있어서 PM도 포함해서 미팅 진행해봐도 될 듯 합니다 👍

### 질문 정리

- 프로모션은 어떻게 관리되는건지. 프론트에서 이미지 가지고 있는 걸까요?
- 쿠폰 관련은 MVP에서 제외되는걸로 생각해도 될까요?
- 타임딜 재고는 그냥 생성하는 걸로?
- `FE` 소셜로그인 구현 방식 어떻게 생각하고 계셨는지
    1. 백엔드가 Spring security로 리다이렉트까지 처리 → 프론트는 이동/콜백 처리만
    2. 프론트가 SNS SDK로 직접 인가코드 받아서 백엔드 API로 넘겨주는 방식 — 프론트가 SNS 연동 로직을 직접 다룸
- `FE` 사진 저장 방식
    1. presigned URL 방식
    2. 벡엔드가 파일을 직접 받아 올리는지

## 3. 피드백 및 개선 사항

---

### 강사님 멘토링 질문 목록 (이번주는 설계로 인해 pass)

- 상품 관련 정보 관리 방법?
    
    ![image.png](8%2027%20%EB%8D%B0%EC%9D%BC%EB%A6%AC%EC%8A%A4%ED%81%AC%EB%9F%BC/image.png)
    
    - 목록이나 상세 조회에 연관되는 정보는 어떻게 관리해야될지 (주기적으로 동기화?)
- 목록 조회 등에서 AI 추천순(유저 개인화) ← 한번 계산하고 캐싱 등을 이용해야 할지
- 오늘은 요구사항 정리하면서 설계 진행

## 4. 다음 날 계획

---

- 백오피스 관리자 인증 방식 결정 필요
    - 그냥 하나 생성하고 그거 사용해서 로그인하는 방법으로 해도 ㄱㅊ을 것 같긴 함